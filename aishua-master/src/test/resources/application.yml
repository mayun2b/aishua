spring:
  datasource:
    url: jdbc:h2:mem:aishua;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    driver-class-name: org.h2.Driver
    username: sa
    password:
  data:
    redis:
      host: localhost
      port: 6379

app:
  security:
    jwt:
      secret: test-secret-key-value-with-at-least-32-bytes
      expiration-ms: 86400000
