/**
 * 练习模块包说明：
 * - 控制层：对外接口，负责参数接收与权限边界控制；
 * - 服务层：应用服务编排，负责练习会话、草稿与提交流程；
 * - 数据层：实体对象与数据访问；
 * - 支撑层：可复用规则与组件（如判分、统计更新等）。
 * 约定：业务编排优先放在服务层，细粒度规则优先下沉到支撑层，避免单个服务类持续膨胀。
 */
package zysy.iflytek.aishua.modules.practice;
